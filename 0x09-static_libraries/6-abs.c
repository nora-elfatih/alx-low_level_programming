#include "main.h"

/**
 * _abs - Print absolute value of an integer
 *
 * @i: Integer to compute abolute value for
 *
 * Return: Return the absolute value of int
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
